# SPEC

## 目標

建立單人自用的 LINE 金句收藏 MVP，讓書頁、閱讀器截圖與海報能以最少操作轉成可搜尋、可分類與可回顧的文字資料。

## 輸入

- LINE 圖片：書頁、閱讀器截圖或海報。
- LINE 文字：修改後的正確金句、`回顧`、`說明`、`取消`。
- LINE postback：確認、修改、取消。

## 輸出

- LINE 金句預覽、處理結果與收藏編號。
- Google Sheet 中的 `Quotes` 與 `Events`。
- 僅限本人 Google 帳號開啟的回顧頁。

## v1 行為

- 一張圖片只產生一則金句。
- 有螢光色、底線或框選時，優先擷取最主要標註；沒有標註的書頁選一段代表性金句；海報擷取主要標題或標語。
- 固定分類為：工作學習、健康醫療、人際生活、理財投資、創意靈感、其他。
- 每則產生 2–5 個繁體中文標籤。
- 預覽後必須由使用者確認才寫入 Sheet。
- 修改只重新分類文字，不重新抓取圖片或重跑 vision。
- 待確認內容 10 分鐘後失效。
- 原圖不寫入 Drive、Sheet 或專案檔案。

## 回顧頁

- 最新收藏優先。
- 全文搜尋金句、來源與標籤。
- 依固定分類與標籤篩選。
- 隨機抽取一則收藏。
- v1 僅讀；修改與刪除直接在 Sheet 進行。

## 不在 v1 範圍

- 多使用者、多筆同時 pending。
- 一張圖片擷取多則金句。
- LINE 簽章 relay、Cloudflare 或佇列。
- 原圖保存、事後重跑 vision 或額外 OCR 服務。
- 每日推播、RAG、向量搜尋與統計分析。
- 回顧頁公開分享。

## 驗收條件

### P0

- 非白名單使用者被忽略。
- 同一 webhook event 不重複處理。
- `recentEvents` 最多 100 筆並移除超過一小時的事件。
- Webhook 專案沒有回顧頁 `doGet`；Review 專案沒有 `doPost`。

### P1

- 支援的圖片可產生一則符合 schema 的金句、六類之一與 2–5 個標籤。
- Pending 在送出預覽前保存。
- Reply 失敗時可改用 Push 補送同一預覽。

### P2

- 確認、修改、取消與逾時狀態正確。
- 相同 candidate ID 重複確認只產生一列並回傳同一 quote ID。
- Sheet 寫入失敗不清除 Pending。

### P3

- Review deployment 僅本人 Google 帳號可開啟。
- 搜尋、分類／標籤篩選與隨機抽卡正確。
- 頁面不載入第三方資源。
